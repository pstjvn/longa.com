{
  "$schema": "",
  "title": "SellerBalance",
  "description": "DTO for seller balance sheet",
  "$_extends": "balance",
  "type": "object",
  "properties": {
    "target_acctid": {
      "type": "number",
      "$_name": "accountid"
    },
    "pip_charge": {
      "type": "number",
      "$_name": "charge",
      "description": "The profit charge per point."
    },
    "seller_username": {
      "type": "string",
      "$_name": "username",
      "description": "The username of the signal seller."
    },
    "service_min_balance": {
      "type": "number",
      "$_name": "minimumRequiredBalance",
      "description": "The minimum balance required to use the service."
    },
    "service_paid": {
      "type": "boolean",
      "$_name": "isPaid"
    },
    "subscribed": {
      "type": "boolean",
      "$_name": "isSubscribed"
    }
  },
  "required": "$_all"
}
